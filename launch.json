{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach by Process ID",
            "processId": "${command:PickProcess}"
        },
        {
            "type": "PowerShell",
            "request": "launch",
            "name": "PowerShell Launch (folders-based-on-date-taken.ps1)",
            "script": "${workspaceRoot}/folders-based-on-date-taken.ps1",
            "args": [],
            "cwd": "${workspaceRoot}"
        },
    ]
}